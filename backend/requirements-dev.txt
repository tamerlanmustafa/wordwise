# Dev/test-only dependencies. CI installs these; no heavy ML deps here.
# Keep this lean — if a test needs spacy/torch/etc., add that test behind an
# integration marker and install the full requirements.txt in that job only.

# Linting / formatting
ruff==0.7.4

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Minimal runtime subset needed to import and unit-test pure modules
# (JWT, password hashing, pydantic schemas, FastAPI TestClient).
# Kept in lockstep with requirements.txt versions.
fastapi==0.115.0
httpx==0.27.0
pydantic==2.9.0
pydantic-settings==2.5.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 breaks with bcrypt>=4.1 (see requirements.txt)
python-dotenv==1.0.0

# Import-level deps for test modules that import route/service modules
# (tests/test_srs_routes.py, tests/test_auth_guards.py route-guard checks,
# tests/test_chest_service.py, ...). These are NOT the heavy ML deps —
# spacy/torch/sentence-transformers stay out. `prisma` additionally needs
# `prisma generate` to run before pytest (CI does this) so that
# `prisma.enums` / the generated client exist.
prisma==0.11.0
nltk==3.8.1
pdfplumber==0.11.0
pypdf==4.0.1
beautifulsoup4==4.12.3
email-validator==2.3.0
google-auth==2.25.2
requests==2.32.0
aiohttp==3.10.11
python-multipart==0.0.18
asyncpg==0.29.0
# #115 backdrop sampling. Here rather than behind an integration marker because
# it is a 4 MB manylinux wheel with no ML in it — nothing like spacy/torch — and
# without it the only untested line in backdrop_ink.py would be the decode.
Pillow==12.1.1
