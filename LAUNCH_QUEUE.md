# Launch Queue

Ordered work queue from the 2026-08-20 issue triage, extended by the 2026-08-22 grooming pass
(items 25–37, covering every issue that was still open). **One item per session**, top to bottom.
Run `/next-ticket` in a fresh session to pick up the next `todo` item.

Status values: `todo` · `doing` · `done` · `blocked` · `running` (shipped, still working through a backlog) · `user` (needs you, not the agent) · `dropped` (checked and not worth doing; issue closed with the evidence)

Rules for whoever works this file:
- Take the **first** item that is `todo`. Do not skip ahead, do not batch.
- Read only that item's issue. Do not re-triage, do not read the rest of the queue's issues.
- **Before writing code, validate that one item** (step 2.5 of the `next-ticket` skill): is its premise still true, is its proposed fix the right one, is it worth doing now? Evidence — a prod query, a read of the code — not opinion. Default is proceed; anything else stops and asks the user. The item's *premise* is fair game; its *position in the queue* is not.
- Mark `done` only after the verification commands in `CLAUDE.md` actually pass.
- A `done` row **closes its issue**, with the row's Log line as the closing comment. Three exceptions (step 5 of the skill): the fix isn't committed and pushed yet, the row is only half the issue (`#104a`), or something outside the repo is still pending — a migration to run, a key to rotate. Work left uncommitted gets closed by the next session's opening sweep, so nothing stays open by accident.
- Then **stop** and wait. Do not start the next item.

---

## Tier 1 — dated or one-line

| # | Status | Issue | Work |
|---|--------|-------|------|
| 1 | `done` | #126a | Set `LLM_COST_CAP_USD=60` on `wordwise` + `Worker` (2026-08-20, with user approval). **Raise it before item 19 (#131)** — classifying the UNKNOWN bucket adds ~$34 and would land near $76. |
| 2 | `done` | #133 | `healthcheckPath=/health` live on `wordwise` (dashboard, not `railway.json` — that file is shared with `Worker`, which binds no port). Verified green 2026-08-20. |
| 3 | `done` | #151 | Backfilled `movie_scripts.idioms` — 4,347 of 4,406 cached (2026-08-20). The 59 left are scripts with no cleaned text. |
| 4 | `done` | #104a | Arabic gated behind `preview: true` in `i18n/languages.ts` (2026-08-20). Drop the flag to un-gate after #104b. |

## Tier 2 — what user number one hits

| # | Status | Issue | Work |
|---|--------|-------|------|
| 5 | `done` | #124 | **Shipped, then DISABLED in prod** — `TRANSLATION_WARM_WORKER_ENABLED=0` on `Worker` (2026-08-21, deploy `bb0fc097`) after it spent **~$219 of Google in ~13h and cached nothing**. The containment is the finished work here; **the fix is item 29 (#157)** and the re-enable is gated there. Do not re-open this row. |
| 6 | `done` | #125 | Proxy + cache TMDB server-side; key gone from the client. **Rotate the TMDB key only after the new build is adopted** — old installs and `frontend/` still carry the old one. |
| 7 | `done` | #103 | Converged all four movie CEFR derivations onto `difficulty_score`. ⏳ **Deploy is green as of 2026-08-20 (c07387c) and `/movies/by-level?level=A1` returns 200, so `prisma/manual/2026_08_20_converge_movie_cefr_issue_103.sql` is now unblocked and still UNAPPLIED.** Needs the user to run it; `movies.difficulty_level` + `ix_movies_difficulty` are still in prod. Safe to sit on — an unused column costs nothing. |
| 8 | `done` | #94 | Translation-MCQ distractors must not be near-forms of the correct answer. |
| 9 | `done` | #123 | `Cache-Control` + `ETag` on public immutable endpoints. Cloudflare Cache Rule created 2026-08-20 and verified `HIT`. Its Edge TTL is **"use cache-control if present, bypass if not"** — that setting is what keeps `/by-cefr` and `/api/tmdb/autocomplete` out of the edge. Don't change it. |
| 10 | `user` | #101 | Native-speaker review of es/pt/tr/ru. Needs real speakers. Gate on *promoting* those locales. |

## Tier 3 — correctness and latency at any user count

| # | Status | Issue | Work |
|---|--------|-------|------|
| 11 | `done` | #143 | Enrichment slow path parses a whole script (1.6–2.9s) on the event loop. Highest value in tier. |
| 12 | `done` | #129 | Sentence-worker backlog query: `NOT IN` → `NOT EXISTS`, fix the `LOWER()` index defeat. |
| 13 | `done` | #126b | Time-bound the LLM ledger `SUM()`. Split from #126. Settled/tail split in `services/llm_cost_ledger.py` — one full scan per process, index-only after. **Cap semantics unchanged**: still a lifetime total, still shared across `wordwise` + `Worker`. |
| 14 | `done` | #144 | SRS session start: batch spaCy via `nlp.pipe` + one `run_nlp` hop. Cheapest fix in the repo. |
| 15 | `done` | #145 | Lemma backfill pulls every script's full text into memory. Select two columns. |
| 16 | `done` | #148 | Enable ruff `ASYNC`. **Sequence after 11–15** so it lands green. Note in the commit that ASYNC catches none of #141–#145. |
| 17 | `done` | #134 #135 #138 #137 | **One pass, not four PRs.** Round-trip taxes on hot paths. Lemma rank backfill **APPLIED** to prod 2026-08-21 (28,154 rows, 3.7s) — `lemmas.frequency_rank` is now 100% populated. |

## Tier 4 — data quality

Sequence is load-bearing: what counts as a word → what level it is → the feed that reads it → the schema that stores it.

| # | Status | Issue | Work |
|---|--------|-------|------|
| 18 | `done` | #96 | Recalibrate the lemma purity guard. Upstream of everything below. Pushed 2026-08-22 (`4f5de8e` fix + `93c26cb` eval set). Closed 2026-08-22. Spun off **#158** (over-stripped lemmas), which had to land **before 19**. |
| 18.5 | `done` | #158 | Over-stripped lemmas merged. Prod data **APPLIED** 2026-08-22. Pushed as `8db65aa` + `8e9676e`; #158 closed 2026-08-22. |
| 19 | `done` | #131 | UNKNOWN bucket given an exit path. Prod data **APPLIED** 2026-08-22 (34.8% → 25.2%). Pushed as `687fc57`; #131 closed 2026-08-22. |
| 20 | `done` | #116 | Explore pool measured + tracked; Stage 2 declined on the numbers. Pushed as `b7467d7`, deployed and green on `wordwise`; #116 closed 2026-08-22. |
| 21 | `done` | #127 | Reads converged on the `lemmas` registry; schema normalization **declined** with evidence (see Log). Pushed as `1b7233f`; #127 closed 2026-08-22. |
| 22 | `done` | #104b | RTL code half finished: both unmirrored swipes, 6 unmirrored absolute offsets, digits, fonts, two new source guards. **#104 stays open** — §2 (walk Arabic on an iOS *and* an Android device) and §3 (native-speaker review) can only be done by you, and Arabic stays `preview: true` until they are. |
| 23 | `done` | #98 | App language stored on the account; both user-facing emails localized. Reused `language_preference` — **no migration, nothing to run on prod**. Pushed as `e6b534c`, deployed green on `wordwise` + `Worker`, EAS `preview` updated; #98 closed 2026-08-22. |
| 24 | `done` | #102 | `movies.script_text` dropped and `POST /movies` gated to admin. Pushed as `5f836a9`, deploy `5dc119e5` green, then the SQL **APPLIED** 2026-08-22 — in that order, since the old client still selected the column; #102 closed 2026-08-22. |

## Tier 5 — production correctness

Filed after the 2026-08-20 triage, groomed 2026-08-22. ~~Sequence is load-bearing: the planner's
estimates are wrong before 25.~~ **Corrected 2026-08-22** — item 25 checked and the estimates were
never wrong, so nothing here is gated on it and measurements taken before it stand.

| # | Status | Issue | Work |
|---|--------|-------|------|
| 25 | `dropped` | #155 | **Premise dead — the estimates were never stale.** `pg_class.reltuples` is within 0.3% of both real counts; the NULL `last_analyze` is #118 §3's own Postgres restart zeroing `pg_stat_user_tables`. The durable autovacuum setting it asks for already shipped in #118 §4. Closed 2026-08-22 with the evidence; no prod change. **Voids item 27's gating** — see Log. |
| 26 | `done` | #152 | Batch translate capped at 100 items / 5,000 chars each / 20,000 chars per request, plus a **per-user daily character budget** (default 50,000, `settings.translation_daily_char_budget`) shared with the single-text endpoint — which **mobile does call**, contrary to the issue. Pushed as `2597f17`, deploy green on `wordwise`; nothing to run on prod and no EAS update (backend only); #152 closed 2026-08-23. |
| 27 | `done` | #154 | Snapshot gather runs serially inside one transaction; `vocab_snapshot_age` (warn >36h, fail >72h) makes the silence visible. Cause was **`work_mem=32MB` from #118**, not parallelism or stale stats. Fix is **uncommitted**, so #154 closes on the next session's sweep. ⏳ **Post-deploy check still owed**: a row for the current day in `vocab_coverage_snapshots` *and* no `could not resize shared memory segment` in `railway logs -s Worker`. |
| 28 | `todo` | #153 | Sentence worker's refusal skip-list is in-process, so every Railway deploy re-buys the same ~2,000 LLM refusals. Persist it on `lemmas`, with a revocation path keyed to model/prompt version. |

## Tier 6 — translation spend (gated)

| # | Status | Issue | Work |
|---|--------|-------|------|
| 29 | `blocked` | #157 #124 | The $219 runaway fix. Contained by `TRANSLATION_WARM_WORKER_ENABLED=0` (deploy `bb0fc097`) — **nothing is bleeding**. Blocked on the Google credit decision (case 74635119, ~Aug 26–28). **Do not set the flag to `1`** before this ships *and* a daily character quota + budget alert exist on the Cloud Translation API. |
| 30 | `todo` | #156 | Warm with Haiku instead of DeepL/Google — 8x cheaper, no monthly character wall. **Strictly after #157**: it changes who buys, and inherits the re-buy loop and blind meter unchanged. Offline path only; Haiku is 8–19x slower and must never be reachable from the live path. |

## Tier 7 — mobile + store-facing

| # | Status | Issue | Work |
|---|--------|-------|------|
| 31 | `todo` | #100b | Legal docs print `privacy@wordwise.app` while account deletion uses `privacy@getwordwise.us`, and they're dated "April 2026". A GDPR deletion request may be going to a dead mailbox. `frontend/` is **in scope** here — store-required pages. Translation half stays deferred. |
| 32 | `todo` | #110 | Word-card swipe barely responds to a flick: `deckLogic.swipeDecision` takes no `vx` at all, and its horizontal bias is 1.5 against the home feed's 0.65. Mirror `vx` by `directionSign` or RTL breaks. |
| 33 | `todo` | #111 | Deck controls sit out of thumb reach on tall phones — geometry is a stack of fixed constants. Reuse the viewport-derived pattern in `explore/metrics.ts`. Same file as 32; worth one session. |
| 34 | `todo` | #114 | Animation vs live-action filter on the home feed. **No ingest needed** — `movies.genre` is 96.3% populated (378 animated). It's a JSON array in a VarChar, so match by *contains*, and decide where the 171 genre-less films go. |
| 35 | `todo` | #132 | Posters have no persistent disk cache on Android; swap 24 raw `<Image>` sites onto `expo-image` behind one wrapper. Verification is an offline Android cold start — nothing else proves it. |
| 36 | `todo` | #115 | `backdrop_corner_rgb` ingest so the home card's add glyph picks legible ink. Client half already shipped and tested; this is a column + a resumable 4,583-movie backfill. |
| 37 | `todo` | #108 | Onboarding welcome survey — the first self-reported signal. Already fully groomed with AC and tests. Largest item here; ≤4 questions, skippable, answers as versioned enums. |

---

## Deferred — do not pick these up

**Scaling prerequisites** — deferred 2026-08-22 by decision, and the decision is recorded on each
issue. All three exist only to make a **second replica** correct; at 1 replica the in-process state
is correct and free. Kept open, not closed, because the failure is silent — adding a replica later
doesn't error, it quietly doubles the abuse ceiling and duplicates every alert email.
**Read #149 before ever raising `numReplicas` above 1.**  #149 · #150 · #128

**Product/research** (different kind of work, not this queue): #90 · #65 · #87 · #100a (legal
translation half) · #101 (`user` — see item 10)

---

## Closed in the 2026-08-22 grooming pass

#136 (index drop — prod evidence is the **reverse** of the issue's claim: `ix_sentence_bank_movie_id`
is scanned 4,792x against the composite's 39, so the upside is 30 MB against re-planning the hottest
lookup on the table) · #112 (superseded by the Explore word feed + `TodayWordCard`) · #113 (no
measured latency complaint drives it)

## Closed in the 2026-08-20 sweep

#92 (dup of #89) · #91 (A2 81.8% → 20.2%, superseded by #131) · #105 (script text 98.8% → 1.3% empty) · #107 (dup of #108)

## Closed 2026-08-22 (backfill of every `done` row)

#94 · #123 · #126 · #129 · #133 · #134 · #135 · #137 · #138 · #143 · #144 · #145 · #148 — each closed with its Log line as the comment.

Left open on purpose: **#103** (column-drop SQL unapplied) · **#125** (TMDB key rotation pending) · **#104** (code half done 2026-08-22; the device walk and the native-speaker review are the user's, and Arabic stays gated until both — **do not close this in a sweep**). **#96** was on this list while its fix was uncommitted; both halves are pushed as of 2026-08-22, so it is eligible to close on the next sweep. From here `/next-ticket` closes rows itself — step 1 sweeps, step 5 closes.

## Log

| Date | Item | Outcome |
|------|------|---------|
| 2026-08-23 | 27 (#154) | **Premise held; the issue names the wrong query and the wrong mechanism, and the real cause is #118.** Still failing every 15-minute cycle at 10:41Z today, hole now **5 days 8 hours** (28 rows, newest 2026-08-18 02:24Z). But the query it blames — the first in `_gather_raw`, the `movie_lemma_mappings` count — runs in **2.0s**; the one that actually dies is **`orphan_sentences`**, the `sentence_bank` anti-join, 7th of 10. And the mechanism is not "parallelism is on": with **`work_mem = '4MB'` and parallelism left on, the same query returns 63,094**; at prod's **32MB it fails**, asking for the same 8 MB segment. `work_mem=32768` reads `source = configuration file` — it is #118's tuning — and the last good snapshot is 02:24Z on **2026-08-18**, the morning #118 restarted Postgres to apply it. So a bigger `work_mem` raised the ceiling on how large a Parallel Hash's shared scratch area may grow, past what the Postgres container's `/dev/shm` can serve. **Fix is `SET LOCAL max_parallel_workers_per_gather = 0` for the whole gather, not a lower `work_mem`**: a serial plan asks for no shared segment at all, so it cannot come back as the tables grow, and it costs ~1.4s on a once-a-day admin path (full gather **~3.2s** of server time, measured). Scope ends at COMMIT — a plain `SET` would stick to a pooled connection and quietly de-parallelise request-path queries that borrow it. **The trap that nearly made the fix a silent no-op, and the reason every count moved inside the transaction:** Prisma runs raw SQL as a prepared statement cached per connection, and Postgres fixes that plan at first preparation — it does **not** re-plan when a planner setting changes later. Measured on prod, twice: with the parallel plan already cached on the connection, the identical SQL **still fails inside the transaction with the setting confirmed at `0`**, while a byte-different text (one added comment) returns 63,094 in that same transaction; and conversely, once first prepared serially it keeps returning 63,094 when run outside the transaction with parallelism back at 2. So the setting is issued **before** the first count and every count is issued from inside that transaction and nowhere else; a test pins both, and `_gather_counts` is private with one caller for that reason. Prisma's default 5s transaction budget is also raised to 2 minutes — 3.2s is close enough to the default to have reinstated the outage as a timeout. **Audible half:** `vocab_snapshot_age` on `/admin/health/vocab-coverage` in #130's metric shape, warn >36h / fail >72h, so a stale writer alone turns the whole card red with every vocabulary number healthy. It leads a new first tab (**Reporting**) rather than falling into the app's unnamed "Other" bucket, because every "since last snapshot" arrow on that screen is measured against the row it reports on. No snapshot at all **warns and cannot fail** — a fresh database and a writer that never ran are indistinguishable from there. **The `except Exception` at the call site stays exactly as it was** (a metric must never stop sentence generation); it moved into `write_coverage_snapshot_if_due` only so a test can prove a raising snapshot returns `False` and logs instead of aborting the cycle. **Verified end-to-end read-only against prod**: the fixed `compute_vocab_coverage` computes the whole report in 5.13s from a laptop, `vocab_snapshot_age` **128.74h → fail**, overall **fail**; `vocab_coverage_snapshots` still holds 28 rows, nothing written. All six guards mutation-checked (setting after the counts, no transaction, default timeout, metric removed, worker re-raising, category emptied). **2026-08-19 through 2026-08-23 are permanently missing** — point-in-time measurements of a moving corpus, not reconstructible, and deliberately not synthesized. **Residual the broken query was hiding:** `orphan_sentences` is **63,094**, and the 2026-08-18 snapshot already recorded 63,094 — unchanged, so it reads WARN, not a regression. **Not taken, and it needs its own ticket:** `work_mem=32MB` is a system-wide latent risk, not a snapshot one — any query the planner runs as a Parallel Hash over enough rows can hit the same wall. No sign of it on the request path today (no such error in `railway logs -s wordwise`), and the durable options are lowering `work_mem`, raising the container's `/dev/shm`, or setting `min_dynamic_shared_memory` — all prod config, all #118's territory. Also noticed, untouched: `feed_pool_min_level` and `unknown_registry_share` have no mobile category and still render under "Other". |
| 2026-08-23 | 26 (#152) | **Premise held exactly as filed, and the per-request caps alone would not have fixed it.** Re-read today: `max_items=2000`, no per-item cap, docstring still promising 100. But 20,000 chars × 30 requests/min is **600,000 chars/minute** against DeepL's 500,000-a-**month** free allowance — so a request-size cap on its own still empties the quota in under a minute, and the issue's optional step 5 is the load-bearing one. **The daily budget is implemented, not declined:** default 50,000 chars per user per UTC day, `utils/char_budget.py`, keyed by user id (both endpoints require auth, so no IP fallback is needed). **Deliberate scope addition: the budget covers `POST /translate` too** — and that turned out to be required, not tidy, because **the issue's "the mobile app never calls `/translate/*`" is false.** `apps/mobile/src/services/api.ts:518` posts to `/translate` from three shipped surfaces: `TodayWordCard` (the word *and* its example sentence), `VocabRow`, and `WordCardDeck`'s fallback when the server sends no aligned gloss. Metering only `/batch` would also have left the single endpoint as the cheaper way to spend the same quota — 120 requests/min × 5,000 chars is a *higher* character rate than the capped batch endpoint. **Sizing, and the honest gap in it:** the heaviest day any account has had through these endpoints is **113 lookups / 701 characters** in 90 days (1,157 rows lifetime, **longest text ever 20 chars** — `user_translation_history`, re-measured 2026-08-23), but that history is words only; `TodayWordCard`'s sentence translations have never been recorded, so the word figures understate a heavy mobile day. 50,000 still covers ~450 card openings at ~110 chars each (word + sentence) and holds one account to 10% of the monthly allowance per day. Because that unmeasured path exists, the budget reads `settings.translation_daily_char_budget` rather than a constant — **if a real user is ever cut off, raise it in Railway without a deploy**; a test pins that it stays operable. The card deck's `sentence` context hint is deliberately **not** metered: DeepL does not bill for `context`, and the deck sends one on every lookup. **Caps and the measurement behind each:** per-item 5,000 (matches the single endpoint; prod's longest cached `source_text` ever is **453**, 0 rows over 5,000 in 24,145); 100 items (the number the docstring always claimed); 20,000 chars/request (100 items averaging 200 — the real caller sends single words, so a full batch measures ~2,400). **The batch endpoint's only caller was measured, not assumed:** frozen `frontend/`'s `useTranslationQueue` already chunks at `MAX_BATCH_SIZE = 100` and sends single lowercase words from the word feed, so every new cap sits above what it sends and the web reader cannot start 422-ing; a test parses that constant out of the `.ts` file so a future tightening below it fails the suite. Mobile calls the single-text endpoint only — no mobile change, so **no EAS update needed**. **Every cap is a field constraint, not a handler check** — pydantic rejects while parsing, which is before `TranslationService.__init__` builds a DeepLClient *and* a GoogleTranslateClient; the budget charge sits above the same constructor. Four rejection tests assert the spy service was never even instantiated, which is the difference between a cost cap and a cosmetic one. **All five guards mutation-checked**: reverting the item cap to 2,000, dropping the per-item constraint, disabling the total-character check, and removing the budget from either endpoint each turn tests red. **Residual found and deliberately not acted on:** `batch_translate` returns `"cached": True` for *every* resolved text including ones it just paid for, so the route's `api_calls` field in the response is **always 0** and the budget cannot meter cache misses — it meters submitted characters instead, which is the half that has to be checked before the money is spent anyway. Fixing that field means changing `batch_translate`'s contract, which the issue puts out of scope. **Honest limit of the fix:** a per-user budget bounds *one* account (the issue's stated threat), not many — accounts are free, so 10 of them still drain a month in a day, and the in-process counter would double per replica (#149). The global bound is the provider quota itself plus #157's meter; that is the ticket that owns it. |
| 2026-08-22 | 25 (#155) | **Premise dead: the issue measured a monitoring counter, not the planner's input.** It quotes `pg_stat_user_tables.n_live_tup` (13,746 / 7,968) as "the planner estimate"; the planner reads `pg_class.reltuples`, which is **5,047,336** and **3,013,205** against real counts of 5,061,775 and 3,019,758 — **0.29%** and **0.22%** off, not 368x. `EXPLAIN SELECT count(*)` confirms it directly (`rows=2103057` per worker × ~2.4 workers). **The NULL `last_analyze` is item 25's own predecessor.** Prod's postmaster started 2026-08-18 11:07:48 — that is **#118 §3 restarting Postgres for `shared_buffers=6GB`** — and PG15+ holds cumulative stats in shared memory, discarding them on an unclean shutdown. Every table shows it: `word_classifications` reports 13,122 lifetime inserts against 4.8M rows. Proof an ANALYZE really did run: `pg_statistic_ext_data` holds populated ndistinct/MCV/dependencies for `stx_sentence_bank_movie_source` and expression stats for `stx_lemmas_lemma_length`, both created by #118 on 2026-08-18 and writable **only** by ANALYZE. **Autovacuum is correctly quiet, not broken** — #118 §4 already set the per-table `autovacuum_analyze_scale_factor = 0.02` that the issue's step 3 asks for, so the threshold is 50 + 0.02×5,047,336 = **100,997** against 20,206 modifications (`sentence_bank`: 60,314 against 7,968); `reloptions` on both is exactly those two scale factors, nothing disables autovacuum, and the insert path needs 1,010,467 against 13,845. **Two residuals found, checked, and deliberately not acted on.** `movie_lemma_mappings.lemma_id` has `n_distinct` 9,131 against a real 42,627, but all four hot paths key on `movie_id` (est 4,388 / real 4,412, 0.5%) and both `lemma_id` consumers — `sentence_worker.py:113`, `vocab_coverage.py:333` — are semi joins that exit at the first match, so the 553-vs-119 error has no beneficiary. And the visibility map is stale (**1,001,063** heap fetches on `movie_lemma_mappings`, **4,124,835** on `sentence_lemma_links`, inside "index-only" scans), but it costs only the admin-only once-daily `vocab_coverage` query — 2.26s total — against 60 + 8,078 heap fetches on the worker's hot backlog query, and the next autovacuum sets it for free. **No ANALYZE run:** it would clear the NULL for a few days and re-arm on the next restart. The durable fix is documentation — `prisma/manual/README.md` gained a "Reading Postgres statistics" section and #118 §3 a pointer comment, so the **catalog** (`pg_class.reltuples`, `pg_stats`, `pg_statistic_ext_data`) gets checked instead of the counters. **Voids item 27 (#154)'s gating**: "do #155 first — it may fix the plan by itself" is false. The coverage query's plan is already correct (parallel index-only scans, accurate estimates, 2.26s), so #154's failure is not stale stats and must be diagnosed on its own. Also retro-corrects **#136's** closing note, which reasoned "both plans came from the broken planner above anyway" — the planner was fine; #136's 30 MB-not-worth-it conclusion stands on its own evidence. |
| 2026-08-22 | Grooming | **Queue extended 24 → 37; every remaining issue triaged, groomed or closed.** All four premises re-checked against prod first, and two moved. **#155 confirmed and unchanged** — `movie_lemma_mappings` est 13,746 / real 5,056,766, `sentence_bank` est 7,968 / real 3,016,782, both still `last_analyze`, `last_autoanalyze` **and** `last_autovacuum` all NULL while the tables beside them are maintained normally; it therefore leads the tier, because every plan in the system is currently chosen against those numbers. **#154 is worse than filed** — the snapshot hole is 5 days, not 3 (newest 2026-08-18 02:24Z, 28 rows total). **#136's evidence turned out backwards and it was closed**: it asserts the composite index is "the one actually being chosen", but `pg_stat_user_indexes` reads `ix_sentence_bank_movie_id` 4,792 scans against `ix_sentence_bank_movie_source` 39 — a prefix index could still serve those lookups, but 30 MB is not worth re-planning the table's hottest access, and both plans came from the broken planner above anyway. **#114 turned out cheaper than expected** — `movies.genre` is already 96.3% populated (4,412/4,583, 378 animated), so it needs a filter, not an ingest; the trap is that genre is a JSON array inside a VarChar, so it is a *contains* test, and 171 films have no genre at all. **#110 and #111 were empty bodies and are now specs**: the deck's `swipeDecision` takes no velocity argument at all while the home feed commits on `|vx| > 0.5`, and its `HORIZONTAL_BIAS` is 1.5 against the feed's 0.65 — two independent reasons a real thumb flick does nothing; and the deck's geometry is a stack of fixed constants (`CARD_HEIGHT` summed from slot heights, `actionsRow marginTop: 14`) while `explore/metrics.ts` already solves exactly that problem by deriving from the viewport. **Closed:** #136, #112 (superseded by the Explore feed), #113. **Deferred by decision, recorded on each issue:** #149/#150/#128 (all three exist only to make a *second* replica correct) and #87 (one vague sentence against a whole-schema question). **#100 split** — the translation half stays deferred, the factual half is queued as item 31, because the privacy policy prints a contact address that account deletion doesn't use. |
| 2026-08-22 | 24 (#102) | **Premise held; the issue's apply order is backwards and that is the one thing to get right.** Prod re-checked today: **4,583 movies, 0 with `script_text`, 0 bytes** (the issue measured 4,526/0 on 2026-07-23 — the count grew, the population didn't). No caller anywhere: `MovieCreate` is referenced only by the endpoint itself, and neither `apps/mobile` nor frozen `frontend/` nor `packages/types` mentions `script_text` in any casing, so the field and the gate can both change without touching a shipping path. **The gate was the live half.** The docstring said "admin only for now" over a `TODO: Add proper admin check` and a plain `get_current_active_user`, so *any* signed-in account could insert a movies row — and, through an unbounded `Optional[str]`, arbitrary-length text into the hottest table. Now `get_admin_user`, which already guards `/api/scripts/{id}` DELETE and the admin reprocess routes. **The issue says "apply to prod before the code lands", per `prisma/manual/README.md` rule 3 — for a DROP that is exactly wrong.** The generated Prisma client SELECTs every column named in `schema.prisma`, so dropping the column while the current deployment is live 500s **every movie read**, not just the write path. The SQL runs *after* the deploy is green, the same sequencing #105 used for its `movie_scripts` drops and #103 for `difficulty_level`; README rule 3 is written for adds, and the file's header now says so. **Two guards, both mutation-checked:** the route-guard test goes red when the dependency is reverted to `get_current_active_user` (it asserts positively — `get_admin_user` *depends on* the active-user check, so a negative assert can never fail), and a new drift test ties the three files that have to agree — `schema.prisma` declaring no `script_text`, the manual SQL containing the idempotent `DROP COLUMN IF EXISTS`, and the `db.movie.create` payload not passing one — red when the field is re-added to the schema. That triple matters because Prisma has no migration history in this repo, so nothing else connects a schema edit to the SQL that realizes it. **Shipped in the safe order, verified at each step:** pushed `5f836a9` → deploy `5dc119e5` SUCCESS → `/movies/1/vocabulary/preview`, `/movies/1/difficulty` and `/movies/by-level?level=A1` all 200 *before* the drop → SQL **APPLIED** → the same three still 200 *after*, column gone from `information_schema`, and a second run of the file is a `NOTICE … skipping` no-op. |
| 2026-08-22 | 23 (#98) | **Reused `language_preference`; the schema question answered itself.** The column is doubly vestigial — its comment says "translation target", but the actual translation target is `native_language` (that is what the #124 warmer reads), and nothing anywhere read `language_preference`. Prod holds **3 users, 2 NULL and 1 `'en'`**, so there is no backfill and no migration: the repurpose is a comment change in `schema.prisma`. It now means *app UI language*, values validated against `utils/ui_languages.py`. **The signup path, not `PATCH /auth/me`, was the one that mattered.** The welcome email is sent from the register handler before the user can ever open Settings, and `LoginScreen.tsx` hard-coded `language_preference: 'en'` — so a Spanish signup was guaranteed an English welcome even with the rest of the feature built. All three signup paths now send the language the screen is rendered in (`getAppLanguage()`); the two OAuth requests gained an `app_language` field that seeds the account only when it has none, so a second device can't overwrite the first device's pin. **Validation is asymmetric on purpose:** signup and OAuth *drop* an unshipped locale, PATCH *rejects* it with 400. A client one release ahead of the server must still be able to create an account; a client sending junk should find out. `''` on PATCH means clear — `None` already means "this PATCH doesn't touch the field", and Settings' "follow my translation language" reset needs a way to say *unset*. **Resolution gained a rule, not a rewrite:** local pin → **account** → translation language → device → `en`. Rules 1 and 2 are the same fact in two places; local wins because it needs no network and is the choice made *here*. `clearExplicitAppLanguage` deliberately takes no server argument — reading the account back there would instantly restore the pin the user just cleared. **A real defect surfaced underneath, and it had to be fixed for any of this to work:** `PATCH /auth/me` returned the raw Prisma object, so FastAPI's serializer looked for snake_case attributes that don't exist on it and **every camelCase-mapped field came back `null`** — `native_language`, `proficiency_level`, `profile_picture_url`, `is_admin`, entitlements. Verified with pydantic directly, not inferred. `handleUserUpdated`'s `?? current` chain was masking it, which is exactly the "edits look like they save but reload back to the old value" the comment there describes. Both return paths now use `UserResponse.model_validate`, same as `GET /me`. `frontend/` never calls PATCH /auth/me, so nothing frozen changes. **Copy: 2 templates × 5 locales, sentences only** — markup stays in `email_service.py`, so a translator never touches a style attribute. Fallback is key-by-key (a half-translated locale renders English sentences, not blanks) and a test asserts every `{placeholder}` survives translation, because a renamed one raises `KeyError` *inside a BackgroundTask* — an email silently never sent. Separate `reset.intro_text` because the HTML says "tap the button below" and the plain-text body has no button. Username is now HTML-escaped; it is the only user-controlled string in the markup. **The durable guard is the parity test:** `locales.test.ts` parses `backend/src/utils/ui_languages.py` and `email_i18n.py` and fails the mobile suite when they drift from `SELECTABLE_UI_LANGUAGES` — mutation-checked by adding `"ar"` to the Python list and watching it go red. Without it, un-gating a locale on the client ships a translated app with English mail and nobody notices until someone reads their inbox. **Arabic deliberately absent server-side** and PATCH rejects `'ar'`: it is `preview: true` until #104's device walk, and a stored Arabic pin would boot an account into unverified RTL. **Not done:** the new email copy is machine-quality like the rest of the locales — it belongs to **#101**'s native-speaker review, which now covers email as well as UI. Also untouched: `/auth/register` and the OAuth responses have the same null-serialization defect as PATCH did (`LoginScreen` reads `data.user.nativeLanguage` and survives on `|| 'en'` fallbacks) — real, but outside this ticket. |
| 2026-08-22 | 22 (#104b) | **The two swipes were broken as filed, and the guard built for them found six more.** `SwipeableRow` confirmed: its backdrops are `justifyContent` inside a `flexDirection: 'row'`, which Yoga **does** mirror, while `swipeActionOnRelease(dx)` reads a physical `dx`, which never does — so an Arabic user uncovers the red "Not interested" panel and commits "Seen it". Both it and `WordCardDeck` now hold a *logical* offset (positive = toward the trailing edge) and convert once, at the transform — `BookmarkRowWrapper`'s proven shape. Three things the issue didn't list travelled with the same bug: the deck's fly-away overlay and the undo card's return flight both fly out and in on physical `FLY_DISTANCE`/`FLY_ROTATE_DEG`, and the footer hint literally says "swipe left = know it", which is the gesture that advances under RTL. The deck's physical node is built **once per key** inside `animRef` next to `focusedOpacity`, for the reason that comment already gives — a multiply node created in render re-attaches a native node mid-drag. **§6's guards are the durable half.** The gesture rule (a file reading `.dx`/`.vx` must also reference `directionSign`) and the absolute-offset rule (flag a `left:`/`right:` with no mirror within 3 lines at the same indent; allowlist the five files the issue documents as geometric — `PosterFlight`, `JourneyNode`, `PracticeTile`, `PracticeBackdrop`, `Confetti`) were **mutation-checked**: removing `directionSign` from `SwipeableRow` and reverting one `start: 16` each turn their test red. The offset rule found **6 real unmirrored positions** — back buttons in `QuizJourneyScreen`, `SetIntroScreen` and `MovieDetailScreen` (×2), `SetIntroScreen`'s set pill, and the floating quiz pill — all of which would have stayed pinned to the left in Arabic. Plus one mismatch inside the deck: the dashed translation placeholder was drawn at `left: 0` while the solid bar it cross-fades into uses `borderStartWidth`, so under RTL the two halves of one animation sat on opposite edges (now `DashedStartBorder`, `start: 0`). **§4 digits: Western**, your call — 14 static strings across 5 Arabic files rewritten, and a locale test now fails on any Arabic-Indic digit in any locale, because the hundreds of `{{count}}` interpolations can only emit Western and one screen showing both was the whole defect. **§5 fonts: deliberate system fallback**, your call — Georgia/Menlo/serif/monospace carry no Arabic, so under RTL both families resolve to the platform face and a mixed Latin/Arabic headline stops rendering in two typefaces. **Not done, and not doable from here:** §2, the device walk on iOS *and* Android, and §3, the native-speaker review — no test in this repo has ever rendered the app mirrored, so everything above is still inference from code. Arabic stays `preview: true` and #104 stays open on those two. §7 is product: Arabic is a UI language but not a translation target, Hebrew is now only translation work, and text overflow is untested in Arabic. |
| 2026-08-22 | 21 (#127) | **Right problem, wrong fix — the "duplication" is an observation log, and normalizing it would have deleted the evidence #131 ran on.** Premise re-verified and still true: 4,832,746 rows / 35,645 lemmas = **135.6×**, 848 MB, **28.0%** of distinct (lemma, level) pairs disagreeing with the registry (was 38.9%) and **10,757** lemmas carrying conflicting levels (was 18,754) — #119/#131/#158 narrowed it without closing it. But the issue's shape (occurrence rows keyed `(script_id, lemma_id)`, level via FK) is wrong twice over: the classifier is genuinely script-sensitive, so `backfill_unknown_bucket.py` recovered 4,094 lemmas last session by **voting over exactly these per-script disagreements** (`angry` B1 in 1,259 scripts vs UNKNOWN in 3) — under the FK that vote does not exist; and the table holds **83,388 distinct surface words** against 35,645 lemmas, which `enrichment.py` uses to build the reader's tap-a-word `word → lemma` map, with 545 lemmas having no registry row to point an FK at. **The live defect was that readers served the observations, not the aggregate**, and that is what shipped. `services/cefr_registry.py` now owns the rule for both sides: `trusted_registry_sql` (one definition of "this `lemmas` row is a real grade") + `registry_levels`. **Saved-words badge**: `DISTINCT ON (wc.word) ORDER BY wc.id DESC` took whichever movie was ingested last — **7,262 words badged differently from Explore**, which already read `lemmas`. **Journey deck**: keyed by surface form, so it taught inflections as their own cards (prod A1 tile 0 was `ares, ass, uss, usses, uses, weddings, bee, songs…`; **3,293 of its 5,664 entries had no registry row** — `hands`, `months`, `passed`, `understands`, `buttoning`, `prettiest`) and, because 13,156 of 56,448 words carry conflicting levels, the same word sat in up to four level decks. Now `a…, about, like, more, also, first…` and **415.7ms → 0.115ms** (28 buffers, index scan on `ix_lemmas_frequency_rank`). Its sibling `_movie_specific_words` already read `lemmas`, so the two halves of one journey session had been sourced differently. **Three traps the convergence itself created, all caught by measuring before shipping:** honest frequency ordering puts function words first, so A1 tile 0 came back `a, and, for, in, of, that, the, to, are, at` — all in `EXCLUDED_A1_WORDS`, which post-filtered would have emptied the page and **404'd the session**, so the stoplist is now a bound parameter *inside* the WHERE clause (A1 only, mirroring `should_keep_word`) and OFFSET keeps meaning the Nth window; `lemmas` also holds idioms/phrasal verbs that `word_classifications` never did, and their `frequency_rank` puts them at the very front, so every tile 0 came back `get in, in time, get on, on time` / `on and on, go on, first of all` — 1,776 of 26,266 trusted rows but nearly all a learner would see, now excluded by the same `^[a-zA-Z]+$` predicate Explore uses; and the registry's trust filter is **load-bearing, not cosmetic** — **5,642 of its 8,639 A2 rows** are `populate_lemma_registry`'s old A2 default, **5,526 of which appear in no script at all**, so a reader that skipped it would have invented an A2 deck two-thirds made of words nothing ever graded. **Not taken:** `movies.py`'s `cefr_distribution`. Converging it measures **4.67ms → 45.95ms** on `/movies/by-cefr` because the `lemmas` join gives up `ix_word_classifications_script_cefr`, and the shipping app never reads the field (`apps/mobile/src/services/api.ts` takes only `unique_words`; only frozen `frontend/` consumes it) — a 10× cost on a hot browse surface for something nobody renders. A static guard in `test_cefr_read_convergence.py` bans any *other* route from reading `cefr_level` off `word_classifications`. **Also not taken, and still open:** the 848 MB itself. Reclaiming it means the schema change, which is why the issue exists — if it is ever wanted, the vote log and the 83,388 surface forms are the two constraints, and #158's `matched_form` work is the precedent for keeping surface evidence. Registry-quality residue the deck now exposes rather than causes: `found`/`thought`/`men` sit in A2 as if they were lemmas, and single letters like `l` clear `^[a-zA-Z]+$` — #96/#158 territory, and a length floor was deliberately not added here (`ego`, `gal`, `foe`, `emu` are real). |
| 2026-08-22 | 20 (#116) | **Stage 1 done, Stage 2 declined on its own gate, and the measurement found the real defect.** Pool depth per level in prod: **A2 6,489 · B1 2,323 · B2 5,124 · C1 8,552** — nothing starved, so the repeat rate the issue worried about is ~0.17 cards per 20-card session at the shallowest level, and **Stage 3 (worker prioritisation) is unnecessary**. Stage 2's `feed_candidates` table is **not built**: the issue gated it on these numbers and the query is 58.7ms / 7,906 buffers on prod, all of it awaited I/O, against a prod-schema change on a shipping path. Two of its six filter rows are also already stale — `cefr_level::text` died with #118 and the `sentence_bank` join with #120. **Question 7 was the live bug.** The cap was one global `ORDER BY frequency_rank LIMIT 4000`, and rank correlates with level, so the easiest level in the band took every slot: over A2+B1+B2+C1 prod returned 1,499 A2 / 1,417 B1 / 1,084 B2 and **0 of C1's 8,552**, and a B2 user's default B2+C1 band returned 2,000 B2 / **0 C1**. Their C1 bucket was empty, so `_page_plan` redistributed the requested C1 share to easier levels and both Explore *and* Word-of-the-Hour silently stopped stretching anyone whose band included a lower level — a user could dial C1 into the mix and never see a C1 card. Now `row_number() OVER (PARTITION BY cefr_level …)` caps **per level** at `limit // len(levels)`; prod returns 1,000 of each. Costs +13ms (58.7 → 72ms) for the partition sort. **Cap stays at 4,000 total on purpose**: the row budget is Python-side deserialization on the event loop, so raising it to deepen each level would buy repeats-per-session with event-loop stalls — it is redistributed, not increased. Eligibility now has one owner (`services/feed_pool.py`) shared by the feed and the report, so the metric can't report depth the feed won't deal; the feed's hidden_words test moved onto the shared case-insensitive fragment (#129) — prod is 100% lowercase in both tables, 22,488 rows either way, so behaviour is identical today. New metric **`feed_pool_min_level`** on `/admin/health/vocab-coverage` + the daily snapshot: shallowest level's stock, warn <1,000, fail <300, detail carries all four. It measures *true* stock, which is deeper than what one request serves — that split is deliberate, since the failure mode being watched is the pool draining, not the cap. |
| 2026-08-22 | 19 (#131) | **The bucket was never 14,819 unplaceable words — it was one branch and one comparison.** 14,447 of 14,819 rows (97.5%) carry confidence 0.9, the signature of `is_proper_noun_or_fantasy_word`, whose rule 1 is `word[0].isupper()`. Two defects fed it. **(1)** `classify_text` kept the *first* surface form it saw per word, so one line-initial `"Baby, I'm home."` classified every `baby` in the script as a name — now it keeps the **least-capitalized** form (`_case_rank`), so a word the script ever writes lowercase never reaches that branch. **(2)** `_upsert_lemmas` resolved conflicts on `max(confidence)`, and the proper-noun branch returns 0.9 while a real wordlist grade returns ≤0.85 — so one capitalized script overwrote the level every other script agreed on, **permanently**, since no frequency-based re-classification can climb past 0.9. UNKNOWN now doesn't compete on confidence at all (`_level_wins`): any real level beats it, it never displaces one. Without this the registry could not self-heal even with (1) fixed. **Prod had already recorded the right answer and the registry ignored it**: `word_classifications` grades `angry` B1 in 1,259 scripts and UNKNOWN in 3, `baby` A2 in 2,596 and UNKNOWN in 668, while `jesus` (2,492) and `york` (1,277) are UNKNOWN in every script — so the backfill votes on those stored grades rather than re-running the classifier. **Re-running it would have been the wrong fix**: fed the bare lemma it grades `jesus` B1, `york`/`washington`/`california` A2 — #91 all over again. Two independent signals cross-checked: of 9,156 lemmas with any real grade, **4** are never written lowercase. Gate `MIN_GRADED_SHARE=0.75` swept across all 14,819 rows — rejects every name and interjection (`john` 0.01, `mary` 0.00, `george` 0.00, `london` 0.02, `christ` 0.02, `whoa` 0.00, `yep` 0.00, `whoo` 0.02), keeps vocabulary (`baby` 0.80, `cowboy` 0.81, `intercept` 0.80, `casino` 0.84, `puppet` 0.85). **`MIN_MOVIES=5` is separately load-bearing**: below it the set turns into transliterated names from foreign-film subtitles (1–2 movie band ≈ ⅔ junk — `aku`, `hui`, `lak`, `rik`, `rix`, `zan`, `minos`), which `lemma_guard` keeps because they're in Webster's 2nd and its foreign check covers es/fr/de/pt/it/ru, not transliterated CJK. Neither length nor `frequency_rank` separates them — `ego`/`gal`/`foe`/`emu` are 3 letters and real, and `photocopier` ranks 72,443 against `goy` 51,286. **Promotion surfaced two slurs the filter didn't know** — `nig` (7 movies) and `goy` (9), graded C1 and invisible only because UNKNOWN hid them; both added to `_SLUR_BASES`. Prod **APPLIED**: 4,094 lemmas promoted in 2.6s, UNKNOWN **14,819 → 10,725 (34.8% → 25.2%)**; `baby` A2, `study` A2, `angry` B1, `pathetic` B1, while `jesus`/`york`/`john`/`mary` stay put. Undo table `backfill_131_unknown_snapshot` (4,094 rows) written in the same transaction as each chunk; the UPDATE is guarded on `cefr_level='UNKNOWN'` so re-runs are idempotent. **Not taken:** `priority_score` is left alone, matching `fix_overstripped_lemmas.py` — its frequency term is a rank *within* one movie, and prod's average UNKNOWN priority is 0.199, between B1 (0.160) and B2 (0.211), so the stored max already includes real-level contributions and shifting it by the CEFR delta would double-count. **Still open, not this ticket:** 10,725 rows remain, of which 4,060 have no real grade at all (genuine names) and 3,041 are capitalized-majority; `don` promotes at B1 off 4,172 `don't` tokenization stems, and `de`/`en`/`un`/`la` survive as dictionary words. |
| 2026-08-22 | 18.5 (#158) | **Right problem, wrong mechanism.** Every number in the issue re-verified unchanged, but the cause is **NLTK's WordNet lemmatizer, not spaCy** — `boss`→`bos`, `discuss`→`discus`, `species`→`specie` are all `morphy`'s `-s` detachment rule landing on a real dictionary word; spaCy gets `boss`/`discuss` right in lowercase and only contributes `cooky`/`fiberglas`. Since `word_classifications` **and** `sentence_lemma_links` both derive from the NLTK path, that is where the links came from. The issue's biggest instance is missing from it entirely: **`pas`, 2,918 movies / 4,666 links, graded A1**, while the real `pass` sat at UNKNOWN with 31 — invisible to the detector because `pas` is in `hidden_words` and the sweep only looked at visible lemmas. Fix is one shared `correct_lemma(word, lemma)` (`services/lemma_normalizer.py`) called by all three lemmatizer sites, **anchored on the surface form** rather than the lemma: `passed`/`passing` start with "pass" so they are corrected, French `Pas` does not so it is not — a lemma-only rule is guaranteed wrong about one of the two, and prod holds 4,532 of the first and 36 of the second under that one key. Frequency gate is load-bearing separately: surface anchoring alone accepts `gasses`→`gass`. Threshold 20x, measured across all 42,621 lemmas — every genuine over-strip clears it, nearest real word is `ros`/Ross at 12x, and `bus`/`gas`/`canvas`/`focus`/`pus` are nowhere near. **Third site was the real multiplier:** `_get_lemma_simple` keys the CEFR wordlists, so cefrj's `boss` (A2) was filed under `bos` — the junk row *inherited the real word's grade* and the real word got none. Prod **APPLIED**: 7 lemmas merged (42,621→42,614), `pass` UNKNOWN/31 → **A1/2,949**, `boss` B1/19 → A1/1,616, `discuss` B1/13 → A1/1,172, `species` A2/1 → B1/362, `cookie` 286→509; `whoo` (1,157 movies) hidden so #131 can't promote it. **Two traps hit while applying, both now fixed in the script:** the Postgres enum is `proficiencylevel`, not Prisma's `CEFRLevel`, and the failed cast left `pas` merged but ungraded — so each proposal now runs in one transaction, with a 5-minute budget because Prisma's 5s default cannot cover `bos`'s 1,771 links. Backfill decides what to repair by calling the **same** `correct_lemma` the write path uses, voting on real `matched_form` tokens, so the two cannot drift. **Residual: `fiberglas`** (7 movies) — its only recorded surface token is the stripped form itself, so there is no evidence to act on and the script correctly declines to guess; new parses produce `fiberglass`. **Not taken, and deliberately:** the pluralia-tantum class the sweep surfaced — `clothe` (1,730 movies, C1), `congratulation` (1,444, A1), `politic`, `physic`, `headquarter`, `mathematic` — is the same user-visible defect from a different cause, but no surface-anchored rule separates it from legitimate singulars like `footstep` (170) or `inhabitant`. Needs its own ticket, not a widened threshold here. |
| 2026-08-22 | 18 (#96) | **Second opinion, built independently — it agrees.** The labelled eval set the issue asked for now exists (`backend/evaluate_lemma_guard.py` + `data/lemma_guard_eval_labels.csv`, 314 lemmas stratified by frequency band, seeded draw, labelled real/obscure/junk/policy), so the row above's "no eval set built" is superseded. Population-weighted, the shipped guard drops **1.9%** of real vocabulary and retains **99.4%** of junk — mode B dominates mode A by ~50x, which is the same conclusion the row above reached from 40 re-parsed scripts (0.97% of tokens), reached from the other direction. The PROPN filter was independently confirmed unsafe (`brown` 779 movies, `golden` 599, `whip` 517, `sergeant` 498 all tagged PROPN; `lemmas.pos` is first-occurrence-wins per `lemmatization_service.py:176`). Near-miss (option 1) is the only rule that meaningfully cuts junk — 99.4%→73.3% — but at 14.8% real dropped, so it stays out. **Ground truth caveat: `hidden_words` is unusable as an answer key** — every row's reason is `auto: <guard reason>`, i.e. the guard's own output. **Both tables are post-purge**, so what the 2026-07-22 purge destroyed is permanently unmeasurable and the issue's 8,594 figure can never be rechecked. **Two classes still open, neither in the issue:** British spellings are rejected wholesale (`odour`, `colourless`, `equalise`, `civilisation`, `metre`, `gruelling`, `globalisation`, `lasagne`) because the dictionary is Webster's 2nd (American); and foreign-dominance misfires on common English — `fond` is dropped as `foreign_fr` across **268 scripts**, plus `dialog`, `logo`, `espresso`. |
| 2026-08-22 | 18 (#96) | **Right problem, wrong fix.** Operating point set explicitly — **precision over recall** — and written into the guard's docstring: teaching junk costs more than silently omitting a real word. Measured cost of that choice on 40 re-parsed prod scripts: `not_in_dict_low_freq` drops **1,257 lemma types = 0.97% of tokens**, and a 90-item random sample is mostly proper nouns, g-dropping (`livin`), typos (`foward`), and inflections spaCy failed to reduce (`uglier`, `freshest`) that would enter as duplicate rows. All five of the issue's options rejected on measurements, one worth remembering: **the PROPN filter it calls "close to free" is unsafe** — prod tags `staircase`, `blacksmith`, `prevention`, `repellent` as PROPN across 8,145 visible rows, because subtitles are title-cased/ALL-CAPS. The real defect was elsewhere: **the two write paths disagreed.** `classify_text` passed the curated wordlists to the guard; `lemmatize_script` passed nothing, so **876 curated entries** were classified but refused registration — **559** after the deliberate profanity/internationalism/orthography exceptions, **358 of them already in 13,406 `word_classifications` rows** (`binoculars`, `stopwatch`, `hideout`, `fjord`, `zap`). Both callers now share one curated set backed by one process-wide classifier (routes/cefr + routes/admin each built their own; two → one). **Trap found while fixing it:** the wordlist short-circuit outranks *well-formedness*, so the raw set admits 176 non-words — `'s`, `mr.`, `etc.`, `3rd`, `1970s`, `km`, `paralyze/paralyse` — exactly the debris `purge_impure_lemmas.py` deleted; in the registry path the rescue now applies to the dictionary/frequency gate only, costing `hmm`/`tv`. Nothing restored, no eval set built, no dictionary swapped. **Spun off #158**: spaCy strips the final `s` off `-ss` words, so `boss` is stored as `bos` (A1, 1,597 movies) and `discuss` as `discus` (A1, 1,159, card 1,725 of the A1 deck) while the real rows starve at 19 and 13 — not a guard defect, both are dictionary words. **Do #158 before 19 (#131)**, which would promote `cooky` and `whoo` out of UNKNOWN. #96 stays open: fix is uncommitted. |
| 2026-08-21 | 17 (#134 #135 #138 #137) | Four round-trip taxes, one pass. `/srs/stats` 8 counts → 1 `GROUP BY` (verified identical on prod user 1: 162/60/54/39/7/2, 0.121ms, 2 buffers). `check_and_unlock` was worse than filed — 18 keys × SELECT+INSERT+re-read = up to **54** serialized calls — now 2 (3 cold); the ±2s `unlocked_at` heuristic that decided "newly unlocked" is gone, replaced by reading state before the write, and `/achievements/me` shares the memoized 18 defs. Upsert proved on scratch Postgres across 3 rounds: an earned badge survives progress falling back to 0 and keeps its original timestamp. JWT now decoded once per request (middleware → `scope["state"]` → dependency), mutation-checked: reverting the dependency makes the test read 4 decodes instead of 2. **#135's premise was wrong** — `check_and_unlock` has exactly one caller (`POST /achievements/check`, from the badge screen), not "every word save and review". **#137 was right problem, wrong table**: its wordfreq fallback measures 0.005ms/word (~3ms for the biggest script), so backfilling 4.83M `word_classifications` rows to delete it is a bad trade and the fallback stays. The real damage is `lemmas.frequency_rank`, which **61.3% of the new-words deck's 23,034-lemma pool lacks** while srs.py ×2 and quiz.py order by it — so the backfill targets `lemmas` (28,154 rows), as #137's own "and/or" allowed. Rank formula existed in two copies; now one `utils/word_frequency.py`. Backfill **APPLIED**: 28,154 rows in 3.7s, `lemmas.frequency_rank` 14,467 → 42,621 of 42,621, deck pool 38.7% → **100%**. |
| 2026-08-21 | 16 (#148) | `ASYNC` on, but the issue's fix was too narrow. Its two `open()` calls are the *last* three lines of `fetch_subtitle_subliminal` — `scan_video`, `download_best_subtitles` (provider HTTP, own sync client) and `save_subtitles` block ahead of them and ASYNC can't see any of it. Prod says this is the hot path, not a fallback: 4,255 of 4,409 scripts are `SUBTITLE_SRT`, newest today, and it's the *first* source `get_or_fetch_script` tries. Whole block extracted to a plain `def` behind `run_cpu` + `cpu_slot(2)`, which clears both findings without a `noqa`. The shed needed care: `Overloaded` now re-raises past both `except Exception` funnels, because returning `None` there means "no provider had this movie" and would let a busy CPU pool park a retrievable film dead (#78 again). Test asserts both halves — shed → transient, real miss → `ScriptNotFoundError`. `per-file-ignores` needs `tests/**` *and* `**/tests/**`: CI lints from `backend/`, lint-staged from the repo root. |
| 2026-08-21 | 15 (#145) | The script text was the smaller half. `POST /cefr/v2/backfill-lemmas` also read all 4.83M `word_classifications` rows (471 MB) into Prisma objects to group them in Python, then issued ~9.6M round trips writing the mappings — hours of it on the event loop, in a background task that is not offloading. All three moved into SQL: `DISTINCT ON` + `GROUP BY` return 35,650 aggregated rows, one chunked jsonb upsert writes them, and the script→movie dict became a JOIN, so `cleaned_script_text` is never detoasted. Verified equivalent on a scratch Postgres by replaying the old Python over the same data: 304 lemmas, 4,692 mappings, 0 disagreements, idempotent on re-run. `DO UPDATE` still touches only `total_movie_count`/`priority_score`, so a re-run can't undo the #91/#119 re-grades. |
| 2026-08-21 | 13 (#126b) | Every LLM call re-summed the whole ledger: 2,547 seq scans / 40.3M rows off a 17,085-row table, `ix_llm_usage_ledger_ts` used zero times. Now split at a cutoff 60s behind `now()` — older rows summed once into process memory, newer ones read live off the ts index, cutoff rolled forward each call. Prod 2.9ms/238 buffers → 0.139ms/7, seq scan gone after the one cold read per process. Still a lifetime total and still cross-process: both containers read the same tail, so neither can overspend behind the other. `GREATEST()` pins the cutoff against a backwards clock step (proved: removing it turns $3.00 into $4.00). Review caught two more — `reset()` racing a cold read undid itself, and the gloss-align fake replayed the balance on every read, which would have doubled the spend on a second cap check. |
| 2026-08-21 | 14 (#144) | Both per-word loops in `/srs/session/start` replaced by `_lemmatize_many` on `nlp.pipe`, awaited through `run_nlp`. Capped at 2 hops (due rows, then padded forms only) and 1 when the queue is full, 0 when empty. Issue's 0.63ms/word confirmed; a 10-word deck is 6.32ms → 1.77ms and now runs off the loop. Verified equivalent on all 161 real prod `user_words`: 0 disagreements vs the old function. No `nlp_slot` — same call as enrichment's bare-word lemma, shedding ~2ms of work would only cost the deck its dedupe. |
| 2026-08-20 | 12 (#129) | Only one of the issue's three points was still live. The 2.6B-row scan died with #120 (subplan is now a 2,491-buffer index-only scan), and its "use NOT EXISTS" fix is the exact rewrite that wedged the worker on 2026-07-22 — that clause stays `NOT IN`, now with an explicit `lemma_id IS NOT NULL` so the silent-idle trap isn't just a schema assumption. The real win was hidden_words: `NOT IN (SELECT LOWER(word) …)` must read all 34,095 rows to build its hash, so no index can help it; correlated, it's ~404 probes of `ix_hidden_words_word_lower`. Prod: 32–40ms → 23ms, seq scan gone. Same fragment now shared with `vocab_coverage`. |
| 2026-08-20 | 11 (#143) | Four spaCy sites in `/sentences/{word}` moved onto the NLP worker; the two whole-script ones now take an `nlp_slot(3)` and shed as `sentences_unavailable` rather than queue. Prod says the slow path is live: 1,856 of 42,594 lemmas (4.4%) have no sentence link and 238 movies have no SentenceBank at all. The issue's per-sentence loop is already dead in prod — `matched_form IS NULL` is 0 of 7.78M links — batched via `nlp.pipe` anyway. |
| 2026-08-20 | 9 (#123) | Five public movie reads now send `Cache-Control`; a new middleware adds a weak `ETag` to any 200 GET that declares a `max-age` and answers `If-None-Match` with 304. `/by-cefr` deliberately excluded — it subtracts the caller's watched/hidden films, so `public` would leak one learner's feed to the next. **The issue's premise was half wrong:** headers alone don't make Cloudflare cache. `/api/tmdb/trending` has sent `public, max-age=3600` since #125 and still returns `cf-cache-status: DYNAMIC` — the edge caches by file extension, so extensionless JSON needs a Cache Rule in the dashboard. Until then the win is device-side + 304s. |
| 2026-08-20 | 8 (#94) | Distractors that contain (or sit inside) the correct translation are filtered out of the grid. Measured on the live TR cache: 0.28% of cards swap a distractor, 0 dropped at the standard 10-card deck; only a 4-word deck can starve, ~1 card in 1,000. Edit distance deliberately rejected — it would have killed fair pairs like `comer`/`correr`. |
| 2026-08-20 | 7 (#103) | Four derivations, not two: only 61% of movies got the same level from all paths, and 418 read B1/B2/C1 at once. The enum was just the score re-bucketed lossily, so it's gone — level is derived from `difficulty_score` on read. Also fixed two silent bugs it was hiding: onboarding's first-film list 400'd for every new user, and the quiz treated every movie as B1. Column drop SQL is written but **must run after the deploy**. |
| 2026-08-20 | 6 (#125) | TMDB moved behind `/api/tmdb/*` with a single-flight TTL cache; a 20-row page is 1 request, not 20, and no key ships in the bundle. Key still hard-coded in frozen `frontend/` — rotation would break those pages. |
| 2026-08-20 | Triage | 45 → 41 open. Queue created. |
| 2026-08-20 | 2 (#133) | `/health` gate live on `wordwise`; first healthchecked deploy passed. Couldn't go in `railway.json` — shared with `Worker`, which binds no port. #133's premise was wrong: Railway healthchecks are deploy-time only, so this gates bad deploys, it does not restart a wedged process. |
| 2026-08-20 | 1 (#126a) | Cap set to $60 on both services. No deadline after all: only 2,048 lemmas left to generate (~$5 at $0.00232/call) against $12.82 headroom — the worker finishes on its own. |
| 2026-08-20 | 3 (#151) | Backfilled 4,316 scripts in 75min, 338,857 idioms, 0 failures. Prod pending 4,321 → 0; no movie now parses a script on the request path. |
| 2026-08-20 | 5 (#124) | Live and warming: TR 19,340 → 20,048, provider-tagged. Prod-only bugs found after deploy: Worker had no `DEEPL_API_KEY` (separate env from `wordwise`), a Railway var was saved as `" GOOGLE_TRANSLATE_ENABLED"` with a leading space, and the DeepL→Google handover tested `<= 0` when DeepL sat at exactly 2 spendable chars — stranding Google's 450k. Fixed in f2ae24e. |
| 2026-08-20 | 5 (#124) | Moved warming into the worker container. Found `movie_jobs` fully drained (4,328 done / 0 pending) — the movie pipeline finished, so `worker`+`controller` were idle; warming is added, nothing stopped. Also fixed: stale `DEEPL_SUPPORTED_TARGET_LANGS` (missing VI/TH/HE), Google creds unusable on Railway (wanted a file path), and Google fallback running 1-at-a-time instead of batched. |
| 2026-08-20 | 5 (#124) | Warmed TR: cache 3,204 → 19,340 rows; DeepL 87,466 → 449,998 of 500,000, reserve intact. Ran without `--tiers`, so it continued past the approved pool scope (284,224 chars) into `tail_lemmas` and spent the full 362,534 budget — pass `--tiers` explicitly next time. |
| 2026-08-20 | 5 (#124) | Built the warmer; run blocked. Prod is on **DeepL Free** (`:fx` key, no `DEEPL_PLAN`): 500k chars/month shared with live traffic, 87,466 already used. Corpus is 4,141,694 chars *per language* — 8× a full month — so #124's "pre-translate the corpus into 5 languages" is unreachable on this plan. Also: targets come from `native_language` (12 options), not the 5 UI locales. Fits today: TR at `--pool-limit 700` = 284,224 chars. |
| 2026-08-20 | 4 (#104a) | Arabic gated with a `preview` flag, not deleted — out of the picker *and* out of language resolution (an Arabic phone would otherwise auto-land in unverified RTL), while its locale files and the RTL guards stay under test. |
